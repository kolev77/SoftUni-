'use strict';
function solve (args) {
    let numbers = args[0].split(' ').map(Number);
    let first = numbers[0];
    let second = numbers[1];
    let third = numbers[2];

    let firstExpression = checkIfEqual(first,second,third;

  if (cfirstExpression){
      console.log()
  }

    function checkIfEqual(first, second, third {
        if (first > second){
            swap (first , second)
        }
        if  (first + second == third) {
            return first + " + " + second + " = " + third

             function  swap (first,second) {
                 let temp = first;
                 first = second;
                 second = temp;

             }
        }
    }
}